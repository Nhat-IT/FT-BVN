//  var [ten ham] = hàm không tên
var a = function(){
    console("var [ten ham] = hàm không tên"); 
}

//  var tenham = hàm không tên rút gọn (arrow function)
var b = () => {
    console.log("var tenham = hàm không tên rút gọn (arrow function)");
}